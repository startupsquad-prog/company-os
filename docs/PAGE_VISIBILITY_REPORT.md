# Page Visibility Report

## Pages with "Coming Soon" Badges Added

The following pages now display a yellow "Coming Soon" badge next to their title:

1. **Messages** (`/messages`) - ✅ Badge added
2. **Calendar** (`/calendar`) - ✅ Badge added
3. **Tickets** (`/tickets`) - ✅ Badge added
4. **Subscriptions** (`/subscriptions`) - ✅ Badge added
5. **CRM Products** (`/crm/products`) - ✅ Badge added
6. **CRM Calls** (`/crm/calls`) - ✅ Badge added
7. **CRM Quotations** (`/crm/quotations`) - ✅ Badge added
8. **CRM Marketing Assets** (`/crm/marketing-assets`) - ✅ Badge added
9. **Documents** (`/documents`) - ✅ Badge added
10. **HR Attendance** (`/hr/attendance`) - ✅ Badge added
11. **Knowledge Base** (`/knowledge/articles`) - ✅ Badge added

## Pages Mentioned in Sidebar But Don't Exist

These pages are listed in the sidebar navigation but the actual page files don't exist:

### CRM Module
- ❌ `/crm/opportunities` - Listed in sidebar but page doesn't exist
- ❌ `/crm/contacts` - Listed in sidebar but page doesn't exist
- ❌ `/crm/companies` - Listed in sidebar but page doesn't exist

### Knowledge Module
- ❌ `/knowledge/sops` - Listed in sidebar but page doesn't exist
- ❌ `/knowledge/files` - Listed in sidebar but page doesn't exist
- ❌ `/knowledge/docs` - Listed in sidebar but page doesn't exist

### Dashboard Module
- ❌ `/dashboard/analytics` - Listed in sidebar (for managers/admins) but page doesn't exist
- ❌ `/dashboard/reports` - Listed in sidebar (for admins/superadmins) but page doesn't exist

### Settings Module
- ❌ `/settings/general` - Listed in sidebar but page doesn't exist
- ❌ `/settings/team` - Listed in sidebar (for managers/admins) but page doesn't exist
- ❌ `/settings/permissions` - Listed in sidebar (for admins/superadmins) but page doesn't exist
- ❌ `/settings/rbac` - Listed in sidebar (for superadmins) but page doesn't exist

### Vertical Modules
- ❌ `/ats` - Listed in sidebar (via NavProjects) but page doesn't exist
- ❌ `/ops` - Listed in sidebar (via NavProjects) but page doesn't exist
- ❌ `/import-ops` - Listed in sidebar (via NavProjects) but page doesn't exist

## Pages That Exist But Are NOT in Sidebar

These pages exist but are not visible in the sidebar navigation:

1. **Dashboard Tasks** (`/dashboard/tasks`) - Page exists but not in sidebar
2. **Test Page** (`/test`) - Page exists (likely for testing, should not be in sidebar)

## Summary

- **Total pages with "Coming Soon" badges**: 11
- **Pages in sidebar but don't exist**: 15
- **Pages that exist but not in sidebar**: 2 (1 is test page)

## Recommendations

1. **Create missing pages** for sidebar items or remove them from sidebar navigation
2. **Add Dashboard Tasks** to sidebar if it should be accessible
3. **Remove or hide test page** from production navigation
4. **Consider adding placeholder pages** with "Coming Soon" badges for missing sidebar items

